import UpdateProfileForm from '../auth/UpdateProfileForm';

const Profile = () => {

    return (
        <>
            <UpdateProfileForm />
        </>
    );
};

export default Profile;
