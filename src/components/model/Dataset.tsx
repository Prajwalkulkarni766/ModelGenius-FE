import { Box } from '@mui/material';
import DatasetList from '../dataset/DatasetList';

const Dataset = () => {
  return (
    <Box>
      <DatasetList />
    </Box>
  );
}

export default Dataset;