export interface Dataset {
  _id: string;
  datasetFilePath: string;
  originalFileName: string;
  fileSize: number;
  projectId: string;
  modelId: string;
  createdAt: string;
  updatedAt: string;
}